[pytest]
markers =
    integration: marks tests that require the ffmpeg-worker Docker container (deselect with '-m "not integration"')
    e2e: marks end-to-end Streamlit tests (deselect with '-m "not e2e"')

testpaths = tests

# Run unit tests only by default (exclude integration & e2e)
addopts = -v
